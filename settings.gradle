rootProject.name = 'whattheburger-project'
dependencyResolutionManagement {
    repositories {
        google()
        maven {
            url 'https://api.mapbox.com/downloads/v2/releases/maven'
        }
    }
}