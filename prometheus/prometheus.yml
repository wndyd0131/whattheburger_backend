global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'whattheburger-backend'
    metrics_path: '/actuator/prometheus' # Actuator 요청 경로
    static_configs:
      - targets: ['backend:8080'] # Docker 사용 기준 (로컬 실행 시 localhost:8080)